# أمثلة استخدام الـ AI Agents

هذا الملف يحتوي على أمثلة عملية لكيفية استخدام كل agent في سيناريوهات مختلفة.

## 🎯 السيناريوهات الشائعة

### السيناريو 1: مراجعة شاملة للمشروع

**الهدف:** الحصول على تقييم كامل للمشروع الحالي

**الخطوات:**

```
المستخدم: "أريد مراجعة شاملة لمشروع Laravel الخاص بي"

الرد:
1. استخدم Laravel Architect للحصول على رؤية معمارية
2. استخدم Laravel Code Quality لتحليل جودة الكود
3. استخدم Laravel Security لمراجعة الأمان
4. استخدم Laravel Performance لتحليل الأداء
5. استخدم Laravel Auditor لتجميع كل التقارير
```

**مثال للطلب:**

```
"باستخدام Laravel Architect، راجع البنية المعمارية الحالية للمشروع مع التركيز على:
- فصل المسؤوليات
- قابلية التوسع
- نقاط الضعف المعمارية"
```

---

### السيناريو 2: تحسين الأداء

**الهدف:** تحديد وإصلاح مشاكل الأداء

**الخطوات:**

```
المستخدم: "التطبيق بطيء جداً، كيف يمكنني تحسين الأداء؟"

الرد:
1. استخدم Laravel Performance لتحديد الاختناقات
2. استخدم Laravel Database Architect للتحقق من استعلامات قاعدة البيانات
3. استخدم Laravel Tech Lead لتطبيق التحسينات
```

**مثال للطلب:**

```
"باستخدام Laravel Performance، حلل المشروع وركز على:
- مشاكل N+1 queries
- الاستعلامات الثقيلة
- فرص استخدام Cache
- العمليات التي يجب نقلها للـ queues"
```

---

### السيناريو 3: تدقيق أمني

**الهدف:** التأكد من أمان التطبيق قبل الإطلاق

**مثال للطلب:**

```
"باستخدام Laravel Security، أجري تدقيقاً أمنياً كاملاً مع التركيز على:
- نقاط ضعف التوثيق والتفويض
- التحقق من صحة المدخلات
- حماية CSRF
- الأسرار المخزنة في الكود
- Routes غير المحمية"
```

---

### السيناريو 4: تحسين جودة الكود

**الهدف:** رفع جودة الكود وتقليل الديون التقنية

**مثال للطلب:**

```
"باستخدام Laravel Code Quality، حلل الكود في مجلد app/Services/ وحدد:
- Code smells الرئيسية
- التكرار في الكود
- الطرق والكلاسات الطويلة جداً
- فرص إعادة الهيكلة"
```

---

### السيناريو 5: تصميم API جديدة

**الهدف:** تصميم API متسقة وقابلة للصيانة

**مثال للطلب:**

```
"باستخدام Laravel API Design، راجع الـ API routes الحالية في routes/api.php وقدم:
- تقييم لتسمية الـ endpoints
- اقتراحات لتحسين الاتساق
- استراتيجية versioning
- تحسينات لـ response structure"
```

---

### السيناريو 6: إعداد CI/CD

**الهدف:** إنشاء pipeline CI/CD موثوق

**مثال للطلب:**

```
"باستخدام Laravel DevOps، ساعدني في إعداد GitHub Actions workflow يتضمن:
- تشغيل الاختبارات تلقائياً
- Code linting و static analysis
- عملية deployment آمنة
- إدارة البيئات (staging/production)"
```

---

### السيناريو 7: تحسين استراتيجية الاختبارات

**الهدف:** زيادة تغطية الاختبارات للميزات الحرجة

**مثال للطلب:**

```
"باستخدام Laravel Testing، راجع استراتيجية الاختبارات الحالية وحدد:
- التدفقات الحرجة التي تحتاج اختبارات
- فجوات التغطية الحالية
- أمثلة لاختبارات يجب كتابتها
- كيفية دمج الاختبارات في CI"
```

---

### السيناريو 8: مراجعة قاعدة البيانات

**الهدف:** إصلاح مشاكل migrations وتحسين schema

**مثال للطلب:**

```
"باستخدام Laravel Database Architect، راجع migrations في database/migrations/ وحدد:
- أخطاء في البنية
- مشاكل في العلاقات
- indexes مفقودة
- تحسينات خاصة بـ PostgreSQL"
```

---

## 🔄 سير عمل متكامل

### مثال: ميزة جديدة من البداية للنهاية

**الميزة:** نظام إدارة الطلبات

#### الخطوة 1: التخطيط المعماري
```
"باستخدام Laravel Architect، ساعدني في تصميم البنية المعمارية لنظام إدارة الطلبات الذي يتضمن:
- جداول: orders, order_items, order_status
- Services, Actions, والـ Controllers المطلوبة
- Events والـ Jobs للعمليات الثقيلة
- استراتيجية المعاملات (transactions)"
```

#### الخطوة 2: تصميم قاعدة البيانات
```
"باستخدام Laravel Database Architect، أنشئ migrations لنظام الطلبات مع:
- العلاقات الصحيحة بين الجداول
- Indexes المناسبة
- Constraints للحفاظ على سلامة البيانات
- تحسينات PostgreSQL"
```

#### الخطوة 3: التنفيذ
```
"باستخدام Laravel Tech Lead، راجع التنفيذ التالي لـ OrderService وقدم ملاحظات حول:
- البنية والتنظيم
- استخدام Laravel best practices
- معالجة الأخطاء
- الأداء"
```

#### الخطوة 4: الاختبارات
```
"باستخدام Laravel Testing، أنشئ استراتيجية اختبارات لنظام الطلبات تتضمن:
- Unit tests للـ OrderService
- Feature tests لـ API endpoints
- Integration tests لعملية الطلب الكاملة"
```

#### الخطوة 5: المراجعة الأمنية
```
"باستخدام Laravel Security، راجع نظام الطلبات وتأكد من:
- صلاحيات الوصول (المستخدم يرى طلباته فقط)
- التحقق من صحة المدخلات
- حماية من التلاعب بالأسعار
- Logging لجميع العمليات الحرجة"
```

---

## 💡 نصائح متقدمة

### نصيحة 1: الجمع بين Agents
يمكنك دمج عدة agents في طلب واحد:

```
"استخدم Laravel Architect لمراجعة البنية، ثم Laravel Security لتحديد المخاطر الأمنية،
وأخيراً Laravel Auditor لتجميع التوصيات في تقرير واحد"
```

### نصيحة 2: التركيز على ملفات محددة
كن محدداً في طلبك:

```
"باستخدام Laravel Code Quality، راجع فقط الملفات التالية:
- app/Services/PaymentService.php
- app/Services/OrderService.php
وركز على روائح الكود والتكرار"
```

### نصيحة 3: طلب تقارير مخصصة
يمكنك طلب تنسيق معين للتقرير:

```
"باستخدام Laravel Performance، أنشئ تقريراً يتضمن:
1. ملخص تنفيذي للإدارة
2. تحليل تقني مفصل
3. خطة عمل مرتبة حسب الأولوية
4. تقدير الوقت لكل تحسين"
```

---

## 📊 متى تستخدم كل Agent

| Agent | استخدمه عندما... |
|-------|-----------------|
| Architect | تبدأ مشروع جديد أو تعيد هيكلة مشروع قائم |
| Tech Lead | تحتاج مراجعة كود أو توجيه لتنفيذ ميزة |
| Code Quality | تريد تحسين جودة الكود أو تقليل الديون التقنية |
| Auditor | تحتاج تقييم شامل أو قبل قرار استراتيجي |
| Security | قبل الإطلاق أو بعد إضافة ميزات حساسة |
| Performance | تلاحظ بطء أو قبل توسيع نطاق التطبيق |
| Database | تعمل على migrations أو تحتاج تحسين queries |
| Testing | تريد زيادة تغطية الاختبارات |
| DevOps | تحتاج إعداد CI/CD أو تحسين عمليات النشر |
| API Design | تصمم API جديدة أو تحسن موجودة |
| Documentation | تحتاج توثيق شامل أو تحديث الوثائق |

---

## 🚀 البدء السريع

للبدء مع أي agent، استخدم الصيغة التالية:

```
"باستخدام [اسم الـ Agent]، [الطلب المحدد] مع التركيز على [النقاط المهمة]"
```

**مثال:**
```
"باستخدام Laravel Architect، راجع البنية المعمارية الحالية مع التركيز على
قابلية التوسع والفصل بين الطبقات"
```

---

**ملاحظة:** جميع الأمثلة أعلاه قابلة للتخصيص حسب احتياجات مشروعك. لا تتردد في تعديلها!
