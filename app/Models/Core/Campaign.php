<?php

namespace App\Models\Core;

use App\Models\Campaign as BaseCampaign;

class Campaign extends BaseCampaign
{
    // This bridge class keeps backward compatibility with legacy namespaces
    // used across the test suite and older CMIS modules.
}
