# 🧠 تعليمات تشغيل سكربت git-ai.sh

يُستخدم سكربت **`/httpdocs/scripts/git-ai.sh`** كمنظّم رئيسي لإدارة جميع عمليات Git في بيئة **CMIS Marketing Limited**.

## 🎯 الهدف
يهدف السكربت إلى جعل عمليات Git تلقائية وآمنة ومنظّمة سواء عند التنفيذ بواسطة البشر أو الذكاء الاصطناعي (AI Agent)، مما يضمن اتساق الأكواد عبر بيئة الخادم وGitHub.

## ⚙️ إعدادات البيئة
قبل أي عملية، يقوم السكربت بتحميل متغيرات البيئة من `.env` بما في ذلك:
- بيانات الخادم (DB، Redis، Cache، إلخ)
- بيانات Git:
  - `GIT_USERNAME`
  - `GIT_EMAIL`
  - `GIT_TOKEN`
  - `GIT_REPOSITORY`

## 🚀 أوامر السكربت

### 🔹 `push`
يدفع جميع التغييرات من الخادم إلى GitHub:
```bash
bash /httpdocs/scripts/git-ai.sh push
```
- يجمع الملفات المعدّلة تلقائيًا (`git add .`)
- ينشئ commit برسالة تلقائية (Auto-sync from server)
- يدفع التحديثات إلى فرع `main`

### 🔹 `pull`
يجلب آخر التحديثات من GitHub إلى الخادم:
```bash
bash /httpdocs/scripts/git-ai.sh pull
```

### 🔹 `status`
يعرض الملفات المعدّلة وغير المضافة إلى Git.

### 🔹 `sync`
يجمع بين `commit`, `rebase`, و`push` في خطوة واحدة لضمان دمج آمن للتغييرات.

### 🔹 `deploy`
ينفذ نشرًا كاملاً عبر GitHub ويتبعه بتحسين بيئة Laravel:
```bash
php artisan migrate --force
php artisan cache:clear
php artisan config:cache
```

### 🔹 `rollback`
يعود إلى الـ commit السابق في حال وجود مشكلة.

### 🔹 `backup`
ينشئ نسخة احتياطية من مجلد `/httpdocs`، ويحذف النسخ الأقدم من 7 أيام.

---

## 🧩 تعليمات الذكاء الاصطناعي (AI Agent)
قبل أي تنفيذ لأي أمر متعلق بـ Git، يجب على الوكيل الذكي (AI Agent):

1. **قراءة هذا الملف** بالكامل: `/app/docs/git/git-ai-instructions.md`
2. **التحقق من وجود ملف `.env`** في المسار `/httpdocs/`
3. **تنفيذ أوامر Git حصريًا عبر السكربت** `/httpdocs/scripts/git-ai.sh`
4. **تجنّب تنفيذ `git` مباشرة** لتفادي أي تضارب في صلاحيات المستخدم أو فروع Git.
5. **توثيق أي عملية push أو deploy** في سجل العمليات المستقبلية داخل `/app/docs/git/`.

---

## 🧱 الهيكل التنظيمي المقترح للمستقبل
```
/app/docs/git/
├── git-ai-instructions.md      ← هذا الملف
├── changelog/                  ← سجل التغييرات الدورية
├── deploy-guidelines.md        ← تعليمات النشر المستقبلي
├── rollback-log.md             ← سجل الاسترجاعات
└── security-policies.md        ← سياسات الأمان والتوقيع الرقمي
```

---
📍 **المسار الحالي:** `/app/docs/git/git-ai-instructions.md`
🕓 **آخر تحديث:** 2025-10-31