## نظام الـ Debug والسجلات

يحتوي النظام على طريقتين لتفعيل التتبّع أثناء التنفيذ:

### 🔹 الوضع العادي (بدون Debug)
يُستخدم في الأوامر الاعتيادية ولا ينتج أي سجل إضافي.

### 🔹 الوضع المفصل `--debug`
يعرض سجلًا مختصرًا في ملف:
```
/httpdocs/storage/logs/laravel.log
```
ويحتوي على:
- عدد الصفحات التي تم جلبها.
- أنواع الوسائط في كل صفحة.
- المدى الزمني للمنشورات.

### 🔹 الوضع الكامل `--debug-full`
يعرض كل تفاصيل الرد من Instagram Graph API بما في ذلك JSON الكامل.

### ⚙️ قراءة السجل
```bash
tail -n 200 /httpdocs/storage/logs/laravel.log | grep "[IG DEBUG]"
```
