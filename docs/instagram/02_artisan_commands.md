## أوامر Artisan الخاصة بـ Instagram

### 🧩 الأمر الرئيسي
```bash
php artisan instagram:api <account> media [خيارات]
```

### ⚙️ الخيارات المتاحة
- `--by=username|id` : لتحديد طريقة التعرف على الحساب.
  - `username` لاستخدام اسم المستخدم كما في الإصدارات السابقة.
  - `id` لاستخدام **account_id العددي** المأخوذ من Meta (مثال: 17841403756121424).
- `--from=<YYYY-MM-DD>` : لتحديد تاريخ البداية.
- `--to=<YYYY-MM-DD>` : لتحديد تاريخ النهاية.
- `--limit=<N>` : عدد المنشورات المطلوبة (افتراضي 25، والحد الأقصى 100 عند تحديد فترة زمنية).
- `--metric=<field>` : الترتيب حسب مقياس محدد مثل `reach` أو `likes`.
- `--sort=desc|asc` : ترتيب النتائج تصاعديًا أو تنازليًا.
- `--debug` : لتفعيل سجل التنفيذ المختصر.
- `--debug-full` : لتفعيل سجل التنفيذ الكامل.

### 💡 أمثلة عملية
```bash
# جلب آخر 3 منشورات باستخدام اسم المستخدم
php artisan instagram:api marketing.limited media --by=username --limit=3

# جلب آخر 3 منشورات باستخدام الـ account_id مباشرة (بعد التحديث)
php artisan instagram:api 17841403756121424 media --by=id --limit=3

# جلب منشورات من أغسطس 2025
php artisan instagram:api marketing.limited media --from=2025-08-01 --to=2025-08-31

# جلب أكثر 5 منشورات تفاعلاً في 2024
php artisan instagram:api marketing.limited media --from=2024-01-01 --to=2024-12-31 --limit=5 --metric=total_interactions --sort=desc
```

### 🧠 ملاحظات إضافية
- عند استخدام `--by=id` يجب أن يكون الحساب مسجلاً في جدول `cmis.integrations` مع عمود `account_id` صالح.
- يدعم الأمر الآن الجلب من واجهة Meta Graph مباشرة عبر الـ ID العددي دون الحاجة إلى UUID.
- التحديث تم إدخاله في إصدار 2025.10.31 عبر commit رقم `91d2455`.
