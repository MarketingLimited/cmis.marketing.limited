## أوامر Artisan الخاصة بـ Instagram

### 🧩 الأمر الرئيسي
```bash
php artisan instagram:api <account> media [خيارات]
```

### ⚙️ الخيارات المتاحة
- `--by=username|id` : تحديد طريقة البحث عن الحساب.
- `--from=<YYYY-MM-DD>` : تحديد تاريخ البداية.
- `--to=<YYYY-MM-DD>` : تحديد تاريخ النهاية.
- `--limit=<N>` : عدد المنشورات المطلوبة (افتراضي 25، ويصل إلى 100 عند تحديد فترة زمنية).
- `--metric=<field>` : الترتيب حسب مقياس محدد (reach، likes، إلخ).
- `--sort=desc|asc` : ترتيب النتائج تصاعديًا أو تنازليًا.
- `--debug` : تفعيل سجل التنفيذ المختصر.
- `--debug-full` : تفعيل سجل التنفيذ الكامل.

### 💡 أمثلة
```bash
# جلب آخر 3 منشورات
php artisan instagram:api marketing.limited media --limit=3

# جلب منشورات من أغسطس 2025
php artisan instagram:api marketing.limited media --from=2025-08-01 --to=2025-08-31

# جلب أكثر 5 منشورات تفاعلًا خلال 2024
php artisan instagram:api marketing.limited media --from=2024-01-01 --to=2024-12-31 --limit=5 --metric=total_interactions --sort=desc
```
