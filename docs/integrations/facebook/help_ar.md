# 📘 مساعدة واجهة برمجة منصة <Platform>

## نظرة عامة
يتيح لك هذا الأمر الوصول المباشر إلى **واجهة برمجة منصة <Platform>** من خلال Artisan.  
يمكنك من جلب البيانات، تحليل الأداء، وتنفيذ المزامنة مع نظام CMIS.

## طريقة الاستخدام
```bash
php artisan <platform>:api [account|page|company] [operation] [options]
```

## الخيارات
| الخيار | الوصف | المثال |
|--------|--------|---------|
| `--limit` | يحدد عدد المنشورات التي سيتم جلبها | `--limit=5` |
| `--from`  | تاريخ البداية (YYYY-MM-DD) | `--from=2025-01-01` |
| `--to`    | تاريخ النهاية (YYYY-MM-DD) | `--to=2025-01-31` |
| `--metric` | المقياس الذي سيتم تحليله | `--metric=engagement` |
| `--sort` | فرز المنشورات حسب مقياس معين | `--sort=likes` |
| `--debug` | عرض سجل تصحيح الأخطاء | `--debug` |
| `--debug-full` | تفعيل سجل تفصيلي | `--debug-full` |

## أمثلة عملية
جلب آخر 10 منشورات:
```bash
php artisan <platform>:api posts --limit=10
```

جلب المنشورات ضمن نطاق زمني محدد:
```bash
php artisan <platform>:api posts --from=2025-01-01 --to=2025-01-31
```

عرض أكثر المنشورات تفاعلاً:
```bash
php artisan <platform>:api posts --metric=engagement --sort=desc
```

---

### 🔹 أمر المزامنة (Sync Command)
لمزامنة بيانات منصة <Platform> مع نظام CMIS:
```bash
php artisan <platform>:sync
```
يقوم هذا الأمر بـ:
- جلب أحدث المنشورات والإحصاءات.
- تخزينها في جداول CMIS (`social_posts`, `social_post_metrics`).
- تحديث مؤشرات الأداء الخاصة بالحساب (KPIs).

---

**نهاية المساعدة الخاصة بالمزامنة**
