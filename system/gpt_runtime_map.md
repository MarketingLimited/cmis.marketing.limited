# 🧠 CMIS GPT Runtime Map

هذا الملف هو المرجع التشغيلي الرسمي الذي يربط بين أنواع الأوامر والخدمات المنفذة من قبل الذكاء الاصطناعي (GPT) عبر أدوات التنفيذ الخارجية.

الغرض: تمكين GPT من فهم أين وكيف ينفذ كل نوع من المهام بناءً على خطة التنفيذ القادمة من الدالة `cmis_dev.prepare_context_execution()`.

---

## ⚙️ خريطة التنفيذ

| نوع المهمة (action_type) | وصف المهمة | أداة التنفيذ المستخدمة | الملاحظات |
|--------------------------|-------------|--------------------------|-------------|
| `sql` | تنفيذ أوامر قواعد البيانات والتحقق من البنية | `data.marketing.limited__jit_plugin.executeSqlQuery` | يُستخدم فقط للأوامر الآمنة مثل SELECT أو INSERT أو UPDATE. يمنع استخدام DROP / DELETE. |
| `api` | استدعاء واجهات Meta / Instagram / Facebook | `data.marketing.limited__jit_plugin.runShellCommand` | عادة يتم تنفيذها باستخدام أوامر `curl` عبر SSH. |
| `artisan` | تشغيل أوامر Laravel Artisan داخل الخادم | `data.marketing.limited__jit_plugin.runShellCommand` | المسار الكامل: `/opt/plesk/php/8.3/bin/php /httpdocs/artisan <command>` |
| `ftp` | رفع أو تنزيل أو حذف الملفات | `data.marketing.limited__jit_plugin.uploadFile` / `downloadFile` / `deleteFile` | المسار الجذر: `/httpdocs/` |
| `analysis` | تحليل النتائج أو تلخيص المعرفة بعد التنفيذ | GPT Logic داخلي | لا يتطلب أداة خارجية، يتم عبر تفسير النتائج وتحليلها. |
| `knowledge` | تحميل المعرفة المرتبطة بالسياق | `SELECT * FROM cmis_knowledge.smart_context_loader()` | تُستخدم لتهيئة الوعي الإدراكي قبل التنفيذ. |

---

## 🔐 قواعد الأمان

1. يمنع تنفيذ أي SQL يحتوي على `DROP`, `ALTER`, `DELETE` إلا بتصريح صريح من الخطة.
2. يجب إنشاء **نسخة احتياطية عبر FTP** قبل أي تعديل على ملفات الإنتاج.
3. يجب تسجيل نتيجة كل خطوة في جدول `cmis_dev.dev_logs` بعد التنفيذ.

---

## 🧩 مخرجات التنفيذ

كل خطة تنفيذ (Execution Plan) يتم التعامل معها بالتسلسل:

```text
[Prepare Context] → [Analyze Intent] → [Execute Steps] → [Validate] → [Update Knowledge]
```

وبعد انتهاء التنفيذ، يجب على GPT تسجيل النتيجة النهائية عبر:

```sql
UPDATE cmis_dev.dev_tasks
SET status = 'completed', confidence = <value>, effectiveness_score = <score>
WHERE task_id = '<uuid>';
```

---

## 📦 موقع الملف

المسار الكامل داخل الخادم:
```
/httpdocs/system/gpt_runtime_map.md
```

الغرض من وجوده: توفير خريطة تشغيل واضحة ومحددة لـ GPT بحيث يعرف كيف يتصرف مع كل خطوة تنفيذية داخل النظام CMIS Orchestrator.