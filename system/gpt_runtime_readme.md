# 🧩 CMIS GPT Runtime — دليل التشغيل الشامل

مرحبًا بك في **CMIS Orchestrator Runtime Environment**، النظام الإدراكي الذي يدمج الذكاء الصناعي (GPT) مع قاعدة البيانات والبيئة التشغيلية (SQL + SSH + FTP) لإدارة التطوير والمعرفة بشكل ذاتي وآمن.

---

## 🧠 الغرض العام

الغرض من هذا النظام هو إنشاء **بيئة تشغيل واعية** يمكن للذكاء الصناعي من خلالها أن:
- يفهم نية المستخدم تلقائيًا.
- يبحث عن المعرفة ذات الصلة من قاعدة البيانات.
- ينشئ المهام والخطط التنفيذية.
- ينفّذ التعليمات عبر الأدوات المدمجة.
- يتحقق من النتائج.
- يتعلم من التجارب السابقة.

كل هذا يجري بطريقة منظمة، مؤرشفة، وآمنة داخل النظام.

---

## ⚙️ مكونات النظام الأساسية

| المجلد | الوظيفة |
|----------|-----------|
| `/httpdocs/system/` | يحتوي جميع ملفات التشغيل الإدراكي الخاصة بـ GPT |
| `/httpdocs/functions/` | يحتوي دوال SQL التي تستخدمها الطبقة التشغيلية |
| `/httpdocs/monitoring/` | ملفات المراقبة والتحليل الصحي للنظام |
| `/httpdocs/maintenance/` | مهام الصيانة التلقائية والنسخ الاحتياطي |
| `/httpdocs/tests/` | بيانات واختبارات النظام الأولية |

---

## 🧩 الملفات الأساسية في `/system`

| الملف | الوظيفة |
|--------|-----------|
| `gpt_runtime_map.md` | خريطة الأدوات والوظائف التي يستخدمها GPT (SQL/SSH/FTP) |
| `gpt_runtime_flow.md` | منطق التشغيل الإدراكي خطوة بخطوة |
| `gpt_runtime_examples.md` | أمثلة تشغيل واقعية (Dev + Marketing + API) |
| `gpt_runtime_errors.md` | آلية التعامل مع الأخطاء والتعافي الذاتي |
| `gpt_runtime_security.md` | سياسات الأمان والتحكم في الوصول |
| `gpt_runtime_audit.md` | نظام التدقيق والتقارير الزمنية |
| `gpt_runtime_dashboard.md` | تصميم لوحة التحكم الحية للمراقبة |

---

## 🚀 تسلسل التهيئة الأولية (Bootstrap Initialization)

1. **تهيئة قاعدة البيانات:**
   ```bash
   psql -d cmis -f /httpdocs/cmis_orchestrator/001_create_schemas.sql
   psql -d cmis -f /httpdocs/cmis_orchestrator/002_knowledge_index.sql
   psql -d cmis -f /httpdocs/cmis_orchestrator/004_dev_tasks.sql
   ```

2. **تحميل الوظائف الأساسية:**
   ```bash
   psql -d cmis -f /httpdocs/functions/load_context_by_priority.sql
   psql -d cmis -f /httpdocs/functions/register_knowledge.sql
   psql -d cmis -f /httpdocs/functions/create_dev_task.sql
   ```

3. **تفعيل نظام المراقبة:**
   ```bash
   psql -d cmis -f /httpdocs/monitoring/health_dashboard.sql
   ```

4. **تفعيل المراقبة الآلية:**
   ```bash
   crontab -e
   # أضف:
   0 2 * * * psql -d cmis -c "SELECT archive_old_logs(90);"
   0 3 * * 0 psql -d cmis -c "SELECT check_stale_knowledge();"
   ```

5. **تجهيز ملفات التشغيل:**
   تأكد من وجود الملفات في `/httpdocs/system/`، حيث يستخدمها GPT لتحديد طريقة التنفيذ، والأمان، والتقارير.

---

## 🧠 دورة التشغيل الإدراكية (Cognitive Runtime Cycle)

1. استقبال طلب المستخدم (Prompt)
2. تحليل النية (Intent Recognition)
3. تحميل المعرفة ذات الصلة (Knowledge Retrieval)
4. التحقق من المهام السابقة (Redundancy Check)
5. إنشاء مهمة جديدة (Task Initialization)
6. تنفيذ الخطوات (Action Execution)
7. التحقق من النتائج (Validation)
8. تسجيل المعرفة الجديدة (Learning Feedback)
9. تحديث السجلات والتقارير (Audit Update)

كل خطوة تُسجل تلقائيًا في:
- `cmis_dev.dev_logs`
- `cmis_audit.activity_log`
- `cmis_audit.security_logs`

---

## 📊 التقارير واللوحة التفاعلية

يمكنك عرض حالة النظام عبر الاستعلام:
```sql
SELECT * FROM cmis_audit.realtime_status;
```
أو فتح لوحة التحكم في `/httpdocs/system/gpt_runtime_dashboard.md` للحصول على تصور بصري كامل لحالة النظام.

---

## 🔐 الأمان وحماية البيانات

قبل أي تنفيذ، يتحقق GPT من:
- صلاحيات الوصول (Access Control)
- سلامة الملفات (File Integrity)
- وجود نسخ احتياطية محدثة (Backups)

كل حادث أمني يتم تسجيله في `cmis_audit.security_logs` ويُعرض في لوحة التحكم.

---

## 🧠 تطوير مستمر (Continuous Intelligence Evolution)

كل تجربة تنفيذية ناجحة تُضاف إلى قاعدة المعرفة:
```sql
SELECT register_knowledge('meta_api','dev','refresh_token_fix','تم تحسين تدفق التوكن بنجاح',2);
```
وهكذا يتطور النظام تلقائيًا مع مرور الوقت.

---

## 🏁 النتيجة النهائية

تم الآن ربط الذكاء الصناعي بالبيئة التشغيلية، فأصبح النظام:
- ذاتي التعلم.
- واعٍ بالسياق.
- موثق بالكامل.
- آمن ومتزن إدراكيًا.

📍 **الموقع:** `/httpdocs/system/gpt_runtime_readme.md`

هذا الملف هو المرجع الرئيسي الذي يربط جميع أجزاء CMIS Orchestrator ويشرح دورة حياته التشغيلية من البداية إلى الذكاء الكامل.