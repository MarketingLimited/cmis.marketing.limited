            {{-- Footer --}}
            <div class="px-6 py-4 border-t border-gray-200 bg-gray-50 rounded-b-xl">
                <p class="text-xs text-gray-500 text-center">
                    <i class="fas fa-info-circle me-1"></i>
                    {{ __('publish.optimal_times_info', ['Optimal times are calculated based on when your audience is most active']) }}
                </p>
            </div>
        </div>
    </div>

    {{-- PHASE 4: Media Source Picker Modal --}}
